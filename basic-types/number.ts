//所有的数字都是浮点数

let decLiteral: number = 6; //支持十进制
let hexLiteral: number = 0xf00d; //支持十六进制
let binaryLiteral: number = 0b1010; //二进制
let octalLiteral: number = 0o744; //八进制

function showNumber() {
    alert(decLiteral);
    alert( hexLiteral);
    alert(binaryLiteral);
    alert(octalLiteral);
}
